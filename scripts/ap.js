// want a function the counts from one to ten

// made a for loop that starts at 1, we need it to go to ten so we set the loop length at 11, then it will iterate in the console until it reaches 10

function bryanTest(){
    for (let i = 1; i < 11; i++ ){
        console.log(i)
    }
}
bryanTest();